<!--
Claude Code reads CLAUDE.md, not AGENTS.md. This file exists only to pull
AGENTS.md into Claude's context so every agent reads the same instructions.
Keep project instructions in AGENTS.md — not here — so they stay shared.
Add content below the import only when it is Claude Code-specific.
(HTML comments are stripped before this file enters context, so this note
costs no tokens.)
-->

@AGENTS.md
